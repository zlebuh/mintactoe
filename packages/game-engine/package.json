{
  "name": "@mintactoe/game-engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc -p tsconfig.test.json",
    "parity-check": "tsx scripts/parity-check.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@vitest/coverage-v8": "latest",
    "fast-check": "latest",
    "tsx": "latest",
    "typescript": "~6.0.2",
    "vitest": "latest"
  }
}
