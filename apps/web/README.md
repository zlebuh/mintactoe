# apps/web

React + TypeScript + Vite frontend for MinTacToe. See the repo root [CLAUDE.md](../../CLAUDE.md) for overall architecture.

```
pnpm --filter web dev      # local dev server
pnpm --filter web build    # production build
pnpm --filter web lint     # oxlint
```
